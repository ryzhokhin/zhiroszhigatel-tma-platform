{
  "name": "zhiroszhigatel-tma-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for the Zhiroszhigatel Telegram Mini App platform.",
  "scripts": {
    "backend:start": "npm --prefix apps/backend start",
    "bot:start": "npm --prefix apps/telegram-bot start",
    "backend:install": "npm --prefix apps/backend install",
    "bot:install": "npm --prefix apps/telegram-bot install",
    "install:all": "npm run backend:install && npm run bot:install"
  }
}
